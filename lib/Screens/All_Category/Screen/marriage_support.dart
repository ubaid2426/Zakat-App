import 'package:flutter/material.dart';

class MarriageSupport extends StatelessWidget {
  const MarriageSupport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Marriage Support");
  }
}